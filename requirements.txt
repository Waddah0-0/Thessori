# Thessori backend dependencies
# Pins loosened from the blueprint's 2024-era versions to floors that ship
# Python 3.14 (cp314) wheels. Same libraries, current stable releases.
# Exact resolved versions are confirmed at `pip install` time (Checkpoint 1).

# Core agent + LLM
langchain-core>=0.3.75
langgraph>=0.2.60
openai>=1.50
pydantic>=2.12

# Web server
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27

# Parsing / utilities
pypdf>=4.3
python-dotenv>=1.0.1
markdown2>=2.4.13

# Google Docs export (optional — the /api/export gdoc path is stubbed for the
# hackathon; these are pure-Python and safe to keep for a future real export).
google-auth>=2.32
google-auth-oauthlib>=1.2
google-api-python-client>=2.139
